{
  "name": "@otto/client",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "oxlint .",
    "format": "prettier . --write",
    "format:check": "prettier . --check"
  },
  "devDependencies": {
    "oxlint": "^1.42.0",
    "prettier": "^3.8.1",
    "vitest": "^4.0.18"
  }
}
