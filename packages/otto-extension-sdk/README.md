# otto-extension-sdk

Shared extension contract library used by Otto core and extension tooling.

This package owns:

- extension manifest schema
- catalog discovery and validation
- diagnostics formatting and validation command helpers
